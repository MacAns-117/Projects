# Models

Running `notebooks/hotel_cancellation.ipynb` writes `random_forest.joblib`
here (sklearn pipeline: one-hot + random forest).

The file is not stored in git. It is about 35 MB and retrains in ~15 seconds.
